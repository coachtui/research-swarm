# Railway Nixpacks configuration
# xhtml2pdf → svglib → rlpycairo → pycairo requires libcairo system headers.
# aptPkgs is additive — does not override Nixpacks' auto-detected Python env.
[phases.setup]
aptPkgs = ["libcairo2-dev", "pkg-config"]
