[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = ["prisma generate --schema=db/schema.prisma"]

[start]
cmd = "uvicorn api.index:app --host 0.0.0.0 --port $PORT"
