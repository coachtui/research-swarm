# API Framework & Serverless
fastapi>=0.109.0
mangum>=0.17.0  # ASGI adapter for Vercel/AWS Lambda
uvicorn[standard]>=0.27.0  # For local development
python-jose[cryptography]>=3.3.0  # JWT handling
passlib[bcrypt]>=1.7.4  # Password hashing

# Background Jobs (Inngest)
inngest>=0.3.0
flask>=3.0.0  # For Inngest HTTP handler

# Cloud Storage (R2/S3)
boto3>=1.34.0  # S3-compatible client
aioboto3>=12.3.0  # Async S3 client

# Authentication (Clerk)
python-clerk>=0.1.0
cryptography>=41.0.0

# Payment Processing (Stripe)
stripe>=7.0.0

# Email (Resend)
resend>=0.7.0

# Monitoring & Logging
sentry-sdk[fastapi]>=1.40.0

# Rate Limiting & Caching
redis>=5.0.1
upstash-redis>=0.15.0  # Serverless Redis

# Include all core research_swarm dependencies
-r requirements.txt

# Development tools
httpx>=0.26.0  # For testing API endpoints
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.1.0
