# Vercel Deployment Requirements - Phase 1
# Minimal dependencies for serverless API with database
# Using Pydantic v1 to avoid Rust compilation issues on Vercel

# Core API Framework (Pydantic v1 compatible versions)
fastapi==0.109.2
mangum>=0.17.0  # ASGI adapter for Vercel serverless
uvicorn[standard]==0.27.1

# Database
prisma>=0.11.0
asyncpg>=0.29.0

# Authentication (JWT for mock auth, ready for Clerk)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Include all core research_swarm dependencies
-r requirements.txt
